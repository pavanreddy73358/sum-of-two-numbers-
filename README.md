//sum-of-two-numbers-
#include<stdio.h>
int main(){
int num1, num2, sum;
printf("Enter the two numbers:");
scanf("%d %d",&num1, &num2);
sum = num1 + num2;
printf("sum of 2 numbers is : %d ",num1, num2, sum);
return 0;
}
